# `bash` auto-runs this for 'interactive login shells', i.e. the top-level shell started
# by the OS/window manager; also the initial shell when `ssh`'ing.
# (And on MacOS: _every_ interactive i.e. non-script-running shell).

. ~/.bashrc
