# See here for available options: https://jupyter-notebook.readthedocs.io/en/stable/config.html
# Or generate a file with defaults: `jup --generate-config`.
# (No worries, it prompts before overwriting this file).

# Some trickery here, apparently: we do not need to import any `c`; it is injected.

c.NotebookApp.open_browser = False
